import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/students/')({
  component: () => <div>Students Management Page (Under Construction)</div>,
})
