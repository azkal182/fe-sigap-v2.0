import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/dormitories/')({
  component: () => <div>Dormitories Management Page (Under Construction)</div>,
})
