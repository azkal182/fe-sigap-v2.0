import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/roles/')({
  component: () => <div>Roles Management Page (Under Construction)</div>,
})
